# Video fixtures

Generate contact sheets with `scripts/video-fixtures/create-contact-sheet.sh`.

Do not commit raw videos, source URLs, local paths, filenames, tokens, or uploader metadata. Committed metadata must be sanitized to duration/dimensions/fps/codec/format/size/has_audio only.
