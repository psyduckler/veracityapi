# Video M1 quality gate — 2026-05-13

Status: **engineering gate passed; production full-path smoke passed; private-beta calibration pending**.

## Scope

Private-beta `/v1/analyze-video` MVP using:

- Direct downloadable HTTPS video URLs only.
- External zero-idle ffmpeg extractor returning one 3x2 JPEG contact sheet plus sanitized metadata.
- Claude Haiku 4.5 vision scoring via Anthropic.
- `$0.05` successful-analysis billing bucket `video_v0`.
- No raw video, frames, contact sheets, base64 payloads, or full URLs stored by default.

## Engineering verification

Passed locally:

```bash
npm test
npx tsc --noEmit
python3 -m py_compile extractors/video-contact-sheet/modal_app.py
npm --prefix packages/mcp test
npm --prefix packages/mcp run build
PYTHONPATH=packages/python/src python3 -m unittest packages/python/tests/test_client.py -v
npm test -- packages/sdk/test/client.test.ts
```

Latest full suite: 17 test files, 147 tests passing.

## Extractor fixture smoke

Generated a local 8-second 720x1280 H.264/AAC fixture and contact sheet with:

```bash
scripts/video-fixtures/create-contact-sheet.sh /tmp/veracity-video-fixture/testsrc.mp4 test/fixtures/video/contact-sheets/m1-synthetic-testsrc
```

Committed artifact policy:

- `m1-synthetic-testsrc.jpg` is a synthetic test-pattern contact sheet.
- `m1-synthetic-testsrc.metadata.json` contains only sanitized codec/duration/dimensions/size/audio metadata.
- No raw video, source URL, local path, filename, token, or uploader metadata is committed.

Sanitized metadata:

```json
{
  "codec_name": "h264",
  "duration_seconds": 8.0,
  "format_name": "mov,mp4,m4a,3gp,3g2,mj2",
  "has_audio": true,
  "height": 1280,
  "size_bytes": 2833430,
  "width": 720
}
```

## Production smoke

Worker deployed successfully after configuring the Modal extractor:

- Version ID: `01bed9df-79f4-4293-a87f-580d5967e4e8`
- Modal extractor deployed as `veracity-video-contact-sheet-extractor`.
- Extractor `/extract` smoke with a small public MP4 returned HTTP `200`, a base64 JPEG contact sheet, sanitized metadata, and sampled timestamps.
- `GET https://api.veracityapi.com/health` => `200`
- Authenticated `POST https://api.veracityapi.com/v1/analyze-video` with `store_content:false` => `200`

Production response summary from the smoke clip:

```json
{
  "analysis_id": "vid_01KRFZ8XT0AFQXS5KSS0MA1H2W",
  "modality": "video",
  "synthetic_video_risk": 0,
  "content_trust_score": 1,
  "risk_level": "low",
  "recommended_action": "allow",
  "confidence": "medium",
  "primary_reason": "sampled_frames_low_visible_synthetic_cues",
  "billing": {
    "units_analyzed": 1,
    "bucket": "video_v0",
    "price_cents": 5,
    "remaining_balance_cents": 495
  }
}
```

D1 verification for the successful production smoke:

- Smoke account balance changed from `500` to `495` cents.
- `usage_events.bucket = 'video_v0'`.
- `usage_events.price_cents = 5`.
- `credit_ledger.type = 'video_analysis_debit'` and `amount_cents = -5`.
- `analysis_logs.kind = 'video'`.
- `analysis_logs.privacy_mode = 1`.
- `analysis_logs.text IS NULL`.
- `analysis_logs.image_url_domain = 'www.w3schools.com'`.
- Full raw video URL was absent from `text`, `context_json`, and `response_json`.

This verifies the live full-path behavior: successful video analyses bill exactly 5 cents and persist only privacy-safe metadata, not raw video/media/contact sheets/full URLs.

## Release decision

- Code/API contract: **ready for private beta**.
- Worker route/discovery/docs/SDK/MCP: **deployed/verified**.
- Production full-path video scoring: **live**.
- Calibration status: **single-smoke engineering gate passed; 20–50 clip private-beta calibration still required before public launch**.

Required before public/wider launch:

1. Run 20–50 labeled clips across real, AI-generated, edited, reposted, and low-quality social videos.
2. Review false positives/false negatives, risk distribution, latency, extractor failures, and wording quality.
3. Tune thresholds/prompt wording if needed.
4. Keep private-beta/non-forensic positioning until calibration is documented.
